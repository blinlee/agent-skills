# Change Log

## 2026-05-22

- Added `skill-creator-pro` project documentation system.
- Recorded routing brief as the active design direction.
- Retired raw full-query matching as the main template selection design.
- Added routing brief CLI and integrated it into composer, selector, and prompt builder paths.
- Added explicit `academic-figure` and `technical-diagram` families.
- Removed matched prompt body injection from final prompts.
- Cleaned retired selector wording from docs, help text, and tests.
- Added golden regressions for scientific schematics and mechanism diagrams.
- Optimized `SKILL.md` to expose routing brief boundaries, routing brief CLI usage, and prompt-intelligence compatibility rules.
- Restored prompt fragment availability for explicit `template-brief --target` usage while preserving canonical-target family inference.
- Added principle inheritance for `academic-figure` and `technical-diagram`.
- Changed final prompt handoff to strict standard JSON prompt strings.
- Removed natural-language composition addons from generated prompts.

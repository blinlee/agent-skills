# Open Issues

## Host LLM Routing Brief

The local CLI now provides a deterministic routing brief and `build-prompt.mjs` accepts `--routing-brief` / `--routing-brief-file`.

Remaining work: define the host-side LLM contract for producing routing briefs before CLI handoff.

## Eval Breadth

The golden set now covers ToF LiDAR, enzyme mechanisms, technical diagrams, financial reports, UI, product, brand, and reference-image paths.

Remaining work: add more hostile cases for biology, chemistry, finance, and mixed UI/product prompts when new failures are found.
